"use strict";

const games = []; // memory array that stores the games temporaraly

module.exports = {
    games
};

/*
it should contain:
id
group
size 
players[] (one if waiting, two if playing)
*/ 